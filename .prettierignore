node_modules/
dist/
coverage/
playwright-report/
test-results/

# Session tooling, not project source
.claude/

# Specification and planning documents: authored prose with deliberate line breaks and
# table alignment. Reflowing them destroys diffs and is forbidden by Plan.md P27 T-27-04.
# Named explicitly so README.md and design-system/*.md are still formatted.
/PRD.md
/SDD.md
/TSD.md
/Plan.md

# Generated catalog data. `npm run seed` writes both files, so prettier must not own their
# formatting: formatting them by hand makes the gate go red after every seed run, which is a
# gate that flaps for a reason unrelated to code. Their shape is asserted by catalog.test.ts
# against mealSchema, which is a stronger check than whitespace.
packages/catalog/meals.json
packages/catalog/nutrition-source.json
