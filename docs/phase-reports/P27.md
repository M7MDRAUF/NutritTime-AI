# P27 — Documentation

**All 5 rows Completed.** T-27-04's discipline held: **fourteen divergences recorded as defects, and
PRD, SDD and TSD are byte-identical.**

---

## 1. T-27-01 — the README did not exist

`README.md` is new: 220 lines, with Node pinned `>=22.13.0 <25` read from the manifests and TSD
§2.1 rather than from a shell, Ollama and `gemma3:4b` stated as **optional** with the deterministic
path and the assistant's unavailable state named, and the fact that **`e2e/` sits outside the npm
workspaces and needs its own install** called out — a clean machine that runs only the root install
finds `test:e2e` broken, which is exactly the class of thing this file exists to prevent.

**Two caveats are in it because a README without them would mislead:**

- **R-59:** `npm run check`'s test total is machine-specific. A reader whose number differs is not
  looking at a broken checkout.
- **R-62:** two documented test commands match no file, so **only commands that were executed** are
  printed.

No secret and no machine-local path: `.env`'s single value was searched for and is absent, and the
USDA archive is referred to by variable name only.

## 2. T-27-04 — fourteen divergences, three new

**Three NEW:**

1. `TSD.md` §2.1 omits `@types/cors`, `@types/node` and `@types/supertest`, which `apps/server`
   declares.
2. `TSD.md` states "Exact versions, not ranges" and then contradicts itself with its own `57.x` and
   `^1.59.1` rows.
3. `design-system/DECISIONS.md` **adopts** "streaming text" while `ollamaClient.ts` sends
   `stream: false` per TSD §5.5 — **document wrong, code right.**

**Eleven already registered**, re-verified rather than re-reported. **X-35 re-resolves to exactly
nine** unresolved "Expected files" paths, unchanged. **X-34 holds and has grown**: `app.ts` now
carries a written rationale for departing from TSD §5.3's middleware order that did not exist when
X-34 was filed. And **P19–P26's Expected-files cells were checked for the first time: all 10 paths
resolve, zero new divergences.**

**A registered row was found FALSE:** R-62 undercounted twice — there are **seven** `features/saved`
suites the dead `saved-meals` filter misses, not six, and the dead `a11y` command appears **twice**,
not once. Its substantive claims all still hold, and its "three test files" figure is exactly right.
Corrected. **That is the sixth recorded row this window found to carry false text**, which is why the
rule is now stated as a rule: a risk row is a hypothesis with a date on it.

**A trap avoided, worth recording:** root `node_modules/zod` is **4.6.4** against a 4.5.4 pin, which
looks like a violation and is not — it is a hoisted dev/peer, while both declaring workspaces carry
4.5.4 nested. A reader checking the pin from the root would have filed a defect that does not exist.

**Verified correct and left alone:** TSD §6.7's sixteen shared components exactly — no seventeenth,
and no `FlatList`, a proposal that was withdrawn once it was pointed out that a framework list
primitive is not a §6.7 component; `IconButton`'s props exactly; and every declared dependency
installed at its declared version.

## 3. The §15.5 mis-citation is finally closed

`PRD §15.5` **does not exist** — the authorities are TSD §5.8 for the rule, PRD §10.3 for the
requirement and Plan §15.5 for the AI lane's row — and the mis-citation once spread to **18 files**.

A full-tree sweep found **exactly one survivor**: `docs/phase-reports/P08.md` still cited "the PRD
15.5 rule" as authority, with no correction marker, inside the record of P08's own verification. All
23 `Plan §15.5` citations are correct and 8 are corrective. Fixed — the sentence now names the real
authorities and records the mis-citation as what it was. **A re-sweep returns zero.**

## 4. T-27-02 — a diff, and it matches

`.env.example` against `config.ts` field for field: **all eight variables and the keep-alive unit
agree.** Only ordering differs, which is not a field. Checked by reading the one file permitted to
touch `process.env`.

## 5. T-27-03 — two false affordances, recorded differently on purpose

`design-system/DECISIONS.md` grew 573 → 711 lines, and the two findings in it are **not** the same
kind of defect:

- **Streaming text was adopted and is architecturally impossible.** `stream: false` is fixed in TSD
  §5.5 and the reply arrives whole, so a typing indicator would animate the model thinking after it
  has finished. The blanket "Adopt" licensed five effects while quoting three of them. Now a
  **Reject** of four, with the original adoption **recorded, not deleted** — a decision log that
  quietly loses a decision is not a log. `design-system/pages/assistant.md` still carries the
  recommendation and is filed, not edited.
- **The dietary-setup error summary was adopted and is simply unbuilt.** Nothing forbids it — and it
  is **implemented on the other form**, at `MealFormScreen.tsx:124-131`, which names this exact
  defect. Verdict left **Adopt**, recorded as adopted-and-unmet with a timestamp.

Collapsing both into "false affordance" would have lost the distinction that **one needs a decision
and the other needs a commit.** The first is now entangled with X-47 (see `P23.md` §7), because the
focus-move half of that same recommendation is what the Plan's focus line forbids.

## 6. T-27-05 — the status columns, and nine `Partial`s

Every P22–P27 row now carries a terminal status: **30 Completed, 9 Partial, 0 Not Started**, counted
from the file rather than asserted.

**The nine are the point.** Every P00–P18 row read `Completed` while six phases were incomplete, and
the re-audit's finding was that the defect was rarely missing behaviour — it was behaviour nothing
would have noticed the loss of. A row marked Completed on half its acceptance is how that happened,
so each Partial names what is missing: T-22-03 (9 of 10 paths), T-22-08 (no lazy loading), T-23-01,
T-23-03 (no screen above `fontScale 1`, no screenshots), T-23-07 (the checklist half), T-25-02
(search and filtering are not local), T-25-04 (warm latency unmeasurable here), T-25-05 (3 of 4
claims), T-26-01 (no Run URL can exist).

**§17.1 reconciles exactly for the first time since P21: 56 rows, 56 files over the cap, 56 agreeing,
0 drifted, 0 missing, 0 dead.** It was **14 drifted and 8 missing** when this window's writers
finished, and it had been measured wrong in three distinct ways at once. It is now reconciled by a
**script** rather than by patching the rows an agent happened to notice — which is how the table
rotted before, and which is also T-28-05's job, made repeatable.

**Two of the eight missing rows were mine**, from edits made during this window: `eslint.config.mjs`
crossing the cap when `scripts/*.mjs` gained the block `e2e/serveExport.mjs` already had, and
`assistantCopy.test.ts` crossing it when the hand-listed denied-claim sweep became a walk.

**A judgement recorded where the document is silent:** SQG-09's cap governs **source**, not markdown.
Its own text says "Every new **file** ≤ 350 lines — PRD §10.4 says files, not only components", and
§10.4 sits in Maintainability beside "Strict TypeScript"; across all 56 rows there is no `.md` row,
while `Plan.md` alone is 3000+ lines. **X-26 remains open and is now load-bearing rather than
editorial**: PRD says "under 350", SQG-09 and T-28-05 say "≤ 350", and **five files sit at exactly
350** — violations needing rows under one document, compliant under the other.

## 7. What could NOT be verified

- **Part 4 is NOT discharged.** "Follow the README on a clean checkout and confirm the app runs"
  needs a clean checkout; no `npm ci`, no build and no gate were run from one. The README's commands
  were each executed **in this tree**, which is weaker and is stated as such.
- **`npm run dev` end to end.** The Windows fix (X-46) was proved in a replica with a byte-identical
  copy of the script and two stand-in commands; the real Expo and API servers were not started
  together, because the ports were in use by the agents driving Playwright.
- **`SDD.md` has no §14.1 and no §21**, so P27's own "Authoritative sources" cell — _"SDD §2.3; §21;
  §14"_ — means **Plan** §21 and §14. The brief that dispatched this work passed the wrong one
  along.
- **Nine "Expected files" paths still do not resolve** (X-35). One decision, not nine, and it is the
  user's.
