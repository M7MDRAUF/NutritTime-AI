# P01 — Repository and Tooling Foundation — Completion Report

**Status:** Completed. **Gate:** green. **Date:** 2026-09-13.

## Tasks

| Task                             | Status                                                                             |
| -------------------------------- | ---------------------------------------------------------------------------------- |
| T-01-01 git init, `.gitignore`   | Completed                                                                          |
| T-01-02 root `package.json`      | Completed                                                                          |
| T-01-03 `tsconfig.base.json`     | Completed — byte-identical to TSD §2.2 (md5 `134d5cdd4579641fa3c752ba7e1a27ca`)    |
| T-01-04 per-package tsconfigs    | **Deferred with Approval** — see Deviations                                        |
| T-01-05 ESLint import boundaries | Completed — 5 of 6 rules enforced and probed; rule 6 unenforceable, see Deviations |
| T-01-06 Prettier                 | Completed                                                                          |
| T-01-07 `vitest.config.mts`      | Completed — three projects                                                         |
| T-01-08 scripts                  | Completed — with three recorded additions                                          |
| T-01-09 `.env.example`           | Completed — 9 variables (8 server + 1 build-time)                                  |
| T-01-10 pinned install           | Completed — 14 pinned devDependencies + `tsx`                                      |

## Files created

`.gitignore` · `.gitattributes` · `.prettierrc` · `.prettierignore` · `.env.example` ·
`package.json` · `package-lock.json` · `tsconfig.base.json` · `tsconfig.json` ·
`eslint.config.mjs` · `vitest.config.mts` · `docs/phase-reports/P01.md`

No `apps/` or `packages/` source exists. No later-phase file was created.

## Gate evidence

```text
prettier --check .          All matched files use Prettier code style!
eslint .                    clean, exit 0
tsc --noEmit -p tsconfig.json   clean, exit 0
vitest run --passWithNoTests    No test files found, exiting with code 0
npm audit --audit-level=high    found 0 vulnerabilities
npm ls --depth=0            14 pinned devDependencies + tsx, all matching TSD §2.1
```

## T-01-05 probe — the acceptance evidence

Nine deliberate violations were written, linted, and removed. Ten errors were reported:

| Probe                                                    | Rule | Result                        |
| -------------------------------------------------------- | ---- | ----------------------------- |
| `packages/domain` imports `express`                      | 1    | error                         |
| `packages/domain` imports `node:fs/promises`             | 1    | error (subpath)               |
| `packages/domain` imports bare `fs`                      | 1    | error                         |
| `packages/domain` uses `new Date()`                      | 1    | error                         |
| `packages/domain` uses `Date.now()`                      | 1    | error                         |
| `packages/domain` imports `../../../apps/server/...`     | 3    | error                         |
| `packages/domain/*.test.ts` imports `express`            | 1    | error (rules apply in tests)  |
| `packages/contracts` imports `lodash`                    | 2    | error                         |
| `packages/contracts` imports `zod`                       | 2    | **clean** (allowlist permits) |
| `apps/server` imports `../../mobile/...`                 | 4    | error                         |
| `apps/mobile` imports AsyncStorage                       | 5    | error                         |
| `asyncStorageDriver.ts` imports AsyncStorage             | 5    | **clean** (exempt)            |
| `asyncStorageDriver.ts` imports `../../../../server/...` | 4    | error (exemption is narrow)   |

An independent verification pass found that the first version of this config **silently disabled
three rules**: later blocks using `'off'` replaced rather than merged the earlier rule, `new Date()`
escaped a `Date.now`-only check, and Node subpath specifiers escaped exact-name bans. All were fixed
and re-probed before this gate was called green.

## Deviations, recorded not silent

1. **Rule 6 (no cyclic imports) is unenforced.** ESLint has no built-in cycle detection; every
   mechanism is a dependency TSD §2.1 does not pin, and SDD §16 declines a cycle checker. Plan.md
   X-10 assumed otherwise on a factual error and is corrected. Cycles are reviewed at P28 T-28-03.
2. **T-01-04 deferred.** A tsconfig whose `include` matches nothing fails with TS18003, and the
   package directories do not exist yet. Each package's tsconfig ships with its package.
3. **`typecheck` deviates from TSD §2.4**, which chains four per-project invocations that cannot run
   before those projects exist. Must return to the per-project form at P12, when `apps/mobile` needs
   its own Expo base and `@/*` paths.
4. **Three script/dependency additions:** `build:server`, `format`, and `tsx` at the root.
5. **`.env.example` has nine variables**, not eight — `USDA_DATASET_PATH` is build-time only, added
   by D-08.
6. **`passWithNoTests` is not in the config.** P01's gate used the CLI flag for one run.

## Known limitations

- `jsdom@30.0.1` declares `node: ^22.22.2 || ^24.15.0 || >=26.0.0`; this machine runs **v24.10.0**, so
  npm emits EBADENGINE. Nothing in P01–P07 uses jsdom — it is first exercised by the `dom` project at
  P12. Recorded as a risk to resolve before then, by raising Node or re-pinning jsdom.
- `eslint@9.39.5` is flagged deprecated by npm. TSD §2.1 pins it; deprecation is not failure.
- `typecheck` currently compiles one file (`vitest.config.mts`), because no source exists. It becomes
  meaningful at P02.

## Blockers

None. B-03 and B-04 are cleared by this phase.

## Next

**P02 — Shared contracts has not been started.**
